name: gemini-continue-image-edit
server: gemini
category: ai-models
description: No description provided
inputSchema:
  type: object
  properties:
    sessionId:
      type: string
      description: The session ID from gemini-start-image-edit
    prompt:
      type: string
      description: Edit instruction (e.g., "make it warmer", "add mountains in the background", "change to night time")
  required:
    - sessionId
    - prompt
  additionalProperties: false
  $schema: http://json-schema.org/draft-07/schema#
example: |-
  await gemini["gemini-continue-image-edit"]({
    sessionId: "<sessionId>",
    prompt: "<prompt>"
  });
